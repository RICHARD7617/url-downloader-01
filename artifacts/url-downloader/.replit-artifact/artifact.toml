kind = "web"
previewPath = "/"
title = "URL Downloader"
version = "1.0.0"
id = "artifacts/url-downloader"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21640

[services.development]
run = "pnpm --filter @workspace/url-downloader run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/url-downloader", "run", "build" ]
publicDir = "artifacts/url-downloader/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21640"
BASE_PATH = "/"
